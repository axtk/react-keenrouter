{
  "name": "@axtk/react-router",
  "version": "1.2.2",
  "description": "A lightweight React router",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild index.js --bundle --outfile=index.build.js --global-name=ReactRouter",
    "build:min": "npm run build -- --minify"
  },
  "keywords": [
    "react",
    "react router",
    "router",
    "history"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/axtk/react-router.git"
  },
  "author": "axtk",
  "license": "MIT",
  "dependencies": {
    "@axtk/router": "^1.2.2"
  },
  "peerDependencies": {
    "react": ">16.8"
  },
  "devDependencies": {
    "esbuild": "^0.8.52"
  },
  "browser": {
    "react": "./external.react.js"
  },
  "publishConfig": {
    "access": "public"
  }
}
